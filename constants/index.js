import keyMirror from 'keymirror'

export default CONST = keyMirror({
    COUNTER_CHANGE: null,
    GET_CURRENT_REPO_COMMITS: null,
    SET_IS_LOADING: null,
    GET_CURRENT_REPO_COMMITS_FAIL: null,

});
